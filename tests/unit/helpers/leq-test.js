import { leq } from 'pulled-pork-blog/helpers/leq';
import { module, test } from 'qunit';

module('Unit | Helper | leq');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = leq([42]);
  assert.ok(result);
});
