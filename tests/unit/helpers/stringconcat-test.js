import { stringconcat } from 'pulled-pork-blog/helpers/stringconcat';
import { module, test } from 'qunit';

module('Unit | Helper | stringconcat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringconcat([42]);
  assert.ok(result);
});
